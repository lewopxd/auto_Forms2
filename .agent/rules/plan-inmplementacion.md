---
trigger: always_on
---

Siempre prioriza una implementación eficiente y rapida, si consideras que la implementacion es grande pregunta si priemero debemos crear un plan.

Si se te pide explicitamente un PLAN no implementes hasta que no se haya validado el plan, crea siempre junto con el plan, dentro del plan, la hoja de ruta, la lista de tareas a ejecutar en la implementacion, y ve actualizando en el plan la hoja de ruta checlist de cada implementacion.